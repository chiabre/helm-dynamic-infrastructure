rbac:
  create: true

serviceAccount:
  create: true
  # name:  # Optional, if empty will be auto-generated

registryKey:
  enabled: false # Deprecated - use imagePullSecrets instead
  registry: https://index.docker.io/v1/
  username: ""
  password: ""

imagePullSecrets: []  # list of existing Secret names for private registries
